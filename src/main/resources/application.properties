# REST: laeuft auf dem Standard-Port
server.port=8080

# GraphQL: GraphiQL Browser-UI aktivieren
# Erreichbar unter: http://localhost:8080/graphiql
spring.graphql.graphiql.enabled=true

# gRPC Server: separater Port (kein HTTP, sondern HTTP/2 binary)
spring.grpc.server.port=9090
